const dinnerGuest:string[] = [
    'zain',
    'karim',
    'rizwan',
]

const numberofPeople = (guest:string[],index:number) =>{
    guest.forEach(guest =>{
        console.log(`Dear${guest} your are invited to dinner`);
        
    })
}
