//with the array you used in Exercise 11, but instead of just printing each person’s name, 
//print a message to them. The text of each message should be the same, 
//but each message should be personalized with the person’s name.


let names = ['murash', 'zain', 'sameer', 'rizwan', 'kashif'];

// Print a personalized message to each person
console.log(`Hello ${names[0]}, would you like to hang out today?`);
console.log(`Hello ${names[1]}, would you like to hang out today?`);
console.log(`Hello ${names[2]}, would you like to hang out today?`);
console.log(`Hello ${names[3]}, would you like to hang out today?`);
console.log(`Hello ${names[4]}, would you like to hang out today?`);
